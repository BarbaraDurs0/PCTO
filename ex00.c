 //creare una variabile di tipo float, inizializzarla ad un valore a vostra scelta
 //stampare la variabile

 #include <stdio.h>

int main()
{
    float a = 17.3805;
    char b = 'R';
    char c = 'S'; 
    int tumadre = 10;

    printf("ho svolto la richiesta con la variabile %c e %c senza eliminare la variabile %f, ma non mi posso scordare di %d\n", b, c, a, tumadre);
}